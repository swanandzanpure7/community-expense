[package]
name = "expense_group"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { version = "21.7.6" }

[features]
# testutils feature intentionally excluded from normal build
# Tests run via: stellar contract invoke + stellar-cli
